#!/bin/bash

. venv/bin/activate

python3 -B -m wandle.main `pwd`/doc/sample.wandle
#python3 -B -m wandle.main `pwd`/doc/readme_example.wandle

#python3 -B -m wandle.main `pwd`/doc/cthulix.wandle
#python3 -B -m wandle.main `pwd`/doc/cthulix_locate.wandle
#python3 -B -m wandle.main `pwd`/doc/hyalos.wandle
#python3 -B -m wandle.main `pwd`/doc/message_paint.wandle


